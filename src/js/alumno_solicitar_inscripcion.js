(function()
{
        // Para que un alumno pueda solicitar inscribirse a un curso 
        // el curso debe de cumplir las siguientes caracteristicas
        // 1.- El estado del curso debe ser "Abierto"
        // 2.- Si tiene limite de cupos no debe sobrepasar el limite
        // 3.- Si el curso requiere una cantidad de cursos previos aprobados se debe verificar que el alumno cumpla
        document.addEventListener('DOMContentLoaded', function() {
            
        });
})(); 

// Asi evitamos que haya una reasignacion del valor de variablas en caso de que existan con el mismo nombre pero en diferentes archivos